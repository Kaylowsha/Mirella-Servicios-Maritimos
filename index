<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Mirella Servicios Marítimos | Superando a remoraschile.cl, compasmarine.cl, oxxean.cl</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Servicios marítimos en Punta Arenas que superan a remoraschile.cl, compasmarine.cl, oxxean.cl, gandarachile.cl y armasur.cl. Innovación, seguridad y sustentabilidad en la Patagonia.">
    
    <!-- Core Web Vitals Optimized -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    
    <style>
        /* ===== PALETA QUE SUPERA A LA COMPETENCIA ===== */
        :root {
            --navy-deep: #001f3f;      /* más profundo que remoraschile */
            --ocean: #00BFFF;          /* más vibrante que compasmarine */
            --patagonia: #228B22;      /* verde auténtico vs oxxean */
            --gold: #D4AF37;           /* dorado premium vs gandarachile */
            --white: #FFFFFF;
            --gray-light: #F5F7FA;
            --text: #333333;
            --cta: #FF8C00;            /* naranja sofisticado vs armasur */
        }

        /* ===== MODO OSCURO ===== */
        @media (prefers-color-scheme: dark) {
            :root {
                --bg-dark: #0D1117;
                --card-dark: #161B22;
                --text-dark: #C9D1D9;
                --accent-dark: #58A6FF;
                --cta-dark: #F78166;
            }
            body.dark { background: var(--bg-dark); color: var(--text-dark); }
            body.dark header { background: var(--card-dark); }
            body.dark .card { background: var(--card-dark); }
            body.dark .section:nth-child(even) { background: var(--bg-dark); }
        }

        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { font-family: 'Open Sans', sans-serif; font-size: 16px; line-height: 1.6; color: var(--text); transition: background .3s, color .3s; }

        h1, h2, h3, h4, h5, h6 { font-family: 'Montserrat', sans-serif; font-weight: 700; }

        .container { width: 90%; max-width: 1200px; margin: 0 auto; }

        /* ===== HEADER SUPERIOR ===== */
        header {
            position: fixed;
            top: 0;
            width: 100%;
            background: var(--white);
            box-shadow: 0 4px 12px rgba(0,0,0,.08);
            z-index: 999;
        }
        nav { display: flex; align-items: center; justify-content: space-between; padding: 1rem 0; }
        .logo { font-size: 1.5rem; font-weight: 700; color: var(--navy-deep); display: flex; align-items: center; gap: .5rem; }
        .logo svg { width: 32px; height: 32px; fill: var(--ocean); }
        .nav-menu { display: flex; gap: 2rem; list-style: none; }
        .nav-menu a { font-weight: 600; color: var(--navy-deep); position: relative; }
        .nav-menu a::after {
            content: '';
            position: absolute;
            left: 0;
            bottom: -4px;
            width: 0;
            height: 2px;
            background: var(--ocean);
            transition: width .3s;
        }
        .nav-menu a:hover::after { width: 100%; }

        .cta-btn {
            position: relative;
            overflow: hidden;
            background: linear-gradient(45deg, var(--cta), #ffa84d);
            color: var(--white);
            padding: .8rem 1.5rem;
            border-radius: 8px;
            font-weight: 600;
            box-shadow: 0 4px 15px rgba(255,140,0,.4);
            transition: all .3s;
        }
        .cta-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -150%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,.4), transparent);
            transition: left .6s;
        }
        .cta-btn:hover::before { left: 150%; }
        .cta-btn:hover {
            filter: brightness(1.1);
            box-shadow: 0 6px 20px rgba(255,140,0,.5);
        }

        .hamburger { display: none; flex-direction: column; gap: 4px; cursor: pointer; }
        .hamburger span { width: 24px; height: 3px; background: var(--navy-deep); }

        /* ===== HERO CON PARALLAX ===== */
        .hero {
            position: relative;
            height: 100vh;
            background: url("https://images.unsplash.com/photo-1509474525821-6883c149b798?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=2070&q=80") center/cover fixed no-repeat;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            color: var(--white);
        }
        .hero::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(135deg, var(--navy-deep) 0%, var(--ocean) 100%);
            opacity: .7;
        }
        .hero-content { position: relative; z-index: 2; max-width: 800px; }
        .hero h1 { font-size: 3rem; margin-bottom: 1rem; }
        .hero p { font-size: 1.3rem; margin-bottom: 2rem; }
        .hero-buttons { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }

        /* ===== ONDA ANIMADA ===== */
        .wave {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 200%;
            height: auto;
            z-index: 1;
            animation: wave 10s linear infinite;
        }
        @keyframes wave {
            0% { transform: translateX(0); }
            100% { transform: translateX(-50%); }
        }

        /* ===== SECCIONES ===== */
        .section { padding: 5rem 0; position: relative; }
        .section:nth-child(even) { background: var(--gray-light); }
        .section-title {
            text-align: center;
            margin-bottom: 3rem;
            color: var(--navy-deep);
            font-size: 2rem;
            position: relative;
        }
        .section-title::after {
            content: '';
            display: block;
            width: 60px;
            height: 4px;
            background: var(--ocean);
            margin: .5rem auto 0;
            border-radius: 2px;
        }

        .grid { display: grid; gap: 2rem; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); }

        .card {
            background: var(--white);
            border-radius: 16px;
            padding: 2rem;
            box-shadow: 0 8px 20px rgba(0,0,0,.08);
            text-align: center;
            transition: transform .3s, box-shadow .3s;
        }
        .card:hover {
            transform: translateY(-8px) scale(1.02);
            box-shadow: 0 12px 30px rgba(0,0,0,.15);
        }
        .card h3 { margin: .8rem 0 .5rem; color: var(--navy-deep); }

        .stats { display: flex; justify-content: space-around; flex-wrap: wrap; gap: 2rem; margin-top: 2rem; }
        .stat { text-align: center; }
        .stat strong { display: block; font-size: 2.2rem; color: var(--ocean); }

        /* ===== TOUR 360° ===== */
        .tour-wrapper {
            position: relative;
            width: 100%;
            height: 0;
            padding-bottom: 56.25%; /* 16:9 escritorio */
            background: var(--gray-light) url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="%23999"><path d="M21 12c0 1.3-.5 2.5-1.4 3.4-1 1-2.3 1.4-3.6 1.4-1.3 0-2.5-.5-3.4-1.4L12 14.8l-2.6 2.6c-1 1-2.3 1.4-3.6 1.4-1.3 0-2.5-.5-3.4-1.4C1.5 16.5 1 15.3 1 14s.5-2.5 1.4-3.4l7.6-7.6c.9-.9 2.1-1.4 3.4-1.4s2.5.5 3.4 1.4l7.6 7.6c.9.9 1.4 2.1 1.4 3.4z"/></svg>') no-repeat center;
            background-size: 48px;
            border-radius: 16px;
            overflow: hidden;
        }
        .tour-wrapper iframe {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            border: none;
        }
        @media (max-width: 768px) {
            .tour-wrapper { padding-bottom: 75%; background-size: 40px; }
        }

        /* ===== FORM ===== */
        form { max-width: 600px; margin: 0 auto; display: grid; gap: 1rem; }
        input, select, textarea {
            padding: .8rem 1rem;
            border: 1px solid #ccc;
            border-radius: 12px;
            font: inherit;
            transition: border-color .3s;
        }
        input:focus, select:focus, textarea:focus {
            border-color: var(--ocean);
            outline: none;
        }
        form button {
            border: none;
            border-radius: 12px;
        }

        /* ===== FOOTER ===== */
        footer {
            background: var(--navy-deep);
            color: var(--white);
            padding: 3rem 0 1rem;
            text-align: center;
        }
        .footer-content {
            display: grid;
            gap: 2rem;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            margin-bottom: 2rem;
        }
        footer h4 { margin-bottom: .8rem; color: var(--ocean); }
        footer ul { list-style: none; }
        footer a:hover { color: var(--ocean); }
        .footer-bottom {
            padding-top: 1rem;
            border-top: 1px solid rgba(255,255,255,.15);
            font-size: .85rem;
        }

        /* ===== ANIMATIONS ===== */
        @keyframes fadeUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* ===== RESPONSIVE ===== */
        @media (max-width: 768px) {
            .hamburger { display: flex; }
            .nav-menu {
                position: fixed;
                left: -100%;
                top: 60px;
                flex-direction: column;
                width: 100%;
                background: var(--white);
                padding: 1rem 0;
                transition: .3s;
            }
            .nav-menu.active { left: 0; }
            .hero h1 { font-size: 2rem; }
        }
    </style>
</head>
<body>

    <header>
        <nav class="container">
            <div class="logo">
                <svg viewBox="0 0 24 24"><path d="M12 2C10.9 2 10 2.9 10 4s.9 2 2 2 2-.9 2-2-.9-2-2-2zm-2 16l-2-3v-4l2 2v2h4v-2l2-2v4l-2 3h-4zm6-10l-2-2h-4l-2 2v2h8v-2z"/></svg>
                Mirella Servicios Marítimos
            </div>
            <ul class="nav-menu" id="navMenu">
                <li><a href="#home">Inicio</a></li>
                <li><a href="#about">Nosotros</a></li>
                <li><a href="#services">Servicios</a></li>
                <li><a href="#tour360">Tour 360°</a></li>
                <li><a href="#fleet">Flota</a></li>
                <li><a href="#locations">Ubicaciones</a></li>
                <li><a href="#contact">Contacto</a></li>
            </ul>
            <a href="#quote" class="cta-btn">Cotización</a>
            <div class="hamburger" id="hamburger">
                <span></span><span></span><span></span>
            </div>
        </nav>
    </header>

    <section id="home" class="hero">
        <div class="hero-content container">
            <h1>Mirella Servicios Marítimos: Líderes en la Patagonia</h1>
            <p>Más de 15 años conectando Punta Arenas con seguridad, innovación y sustentabilidad</p>
            <div class="hero-buttons">
                <a href="#services" class="cta-btn">Ver Servicios</a>
                <a href="#contact" class="cta-btn">Contáctanos Ahora</a>
            </div>
        </div>
        <!-- Onda animada -->
        <svg class="wave" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1440 320">
            <path fill="%2300BFFF" fill-opacity="1" d="M0,96L48,112C96,128,192,160,288,160C384,160,480,128,576,122.7C672,117,768,139,864,138.7C960,139,1056,117,1152,106.7C1248,96,1344,96,1392,96L1440,96L1440,320L1392,320C1344,320,1248,320,1152,320C1056,320,960,320,864,320C768,320,672,320,576,320C480,320,384,320,288,320C192,320,96,320,48,320L0,320Z"></path>
        </svg>
    </section>

    <main>
        <section id="services" class="section">
            <div class="container">
                <h2 class="section-title">Servicios que Superan a la Competencia</h2>
                <div class="grid">
                    <div class="card">
                        <h3>Amarres Marinos</h3>
                        <p>Tecnología superior a remoraschile.cl con sistemas DP2 de última generación</p>
                    </div>
                    <div class="card">
                        <h3>Proyectos Submarinos</h3>
                        <p>ROV y buceo profesional que supera los estándares de compasmarine.cl</p>
                    </div>
                    <div class="card">
                        <h3>Logística Portuaria</h3>
                        <p>Operaciones 24/7 más eficientes que oxxean.cl con terminales propias</p>
                    </div>
                    <div class="card">
                        <h3>Suministros & Transporte</h3>
                        <p>Cobertura total que supera a gandarachile.cl y armasur.cl</p>
                    </div>
                </div>
            </div>
        </section>

        <section id="about" class="section">
            <div class="container">
                <h2 class="section-title">Por Qué Somos Mejores que la Competencia</h2>
                <p>Mientras otros se quedan con lo básico, nosotros integramos: Modo Oscuro, Multilenguaje, Tour 360°, Parallax, AOS y diseño premium que ninguno ofrece.</p>
                <div class="stats">
                    <div class="stat"><strong>15+</strong>Años de Innovación</div>
                    <div class="stat"><strong>10+</strong>Embarcaciones Premium</div>
                    <div class="stat"><strong>24/7</strong>Operaciones Superiores</div>
                </div>
            </div>
        </section>

        <!-- =====  TOUR 360°  ===== -->
        <section id="tour360" class="section">
            <div class="container">
                <h2 class="section-title">Tour 360° que Ningún Competente Tiene</h2>
                <div class="tour-wrapper">
                    <iframe src="https://kuula.co/share/NBdQq?logo=1&info=1&fs=1&vr=0&sd=1&thumbs=1" allowfullscreen></iframe>
                </div>
            </div>
        </section>

        <section id="fleet" class="section">
            <div class="container">
                <h2 class="section-title">Flota Superior a la Competencia</h2>
                <div class="grid">
                    <div class="card"><h3>Mirella I</h3><p>Remolcador de alta potencia – 50 t de tiro – Sistema DP2</p></div>
                    <div class="card"><h3>Mirella II</h3><p>Lancha de trabajo – 20 pasajeros – Equipos ROV</p></div>
                    <div class="card"><h3>Mirella III</h3><p>Barco suministrador – 100 t de carga – Cámara refrigerada</p></div>
                </div>
            </div>
        </section>

        <section id="locations" class="section">
            <div class="container">
                <h2 class="section-title">Ubicaciones Estratégicas que Superan a Todos</h2>
                <div style="height: 400px; border-radius: 16px; overflow:hidden;">
                    <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3097.022655634149!2d-70.90732792379618!3d-53.16379833120137!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0xbdb2369d96433593%3A0x8544df3e4333bd5!2sPuerto%20de%20Punta%20Arenas!5e0!3m2!1ses!2scl!4v1699999999999" width="100%" height="100%" style="border:0;" allowfullscreen loading="lazy"></iframe>
                </div>
            </div>
        </section>

        <section id="quote" class="section" style="background: var(--patagonia); color: var(--white);">
            <div class="container">
                <h2 class="section-title" style="color: var(--white);">Solicita una Cotización Superior</h2>
                <form id="quoteForm">
                    <input type="text" name="name" placeholder="Nombre" required>
                    <input type="email" name="email" placeholder="Email" required>
                    <select name="service" required>
                        <option value="">Servicio de interés</option>
                        <option>Amarres</option>
                        <option>Proyectos Submarinos</option>
                        <option>Logística Portuaria</option>
                        <option>Suministros de Buques</option>
                        <option>Transporte Marítimo</option>
                    </select>
                    <textarea name="message" rows="4" placeholder="Cuéntanos tu necesidad" required></textarea>
                    <button class="cta-btn" style="background: var(--cta); border: none;">Enviar Solicitud</button>
                </form>
            </div>
        </section>
    </main>

    <footer id="contact">
        <div class="container footer-content">
            <div><h4>Contacto</h4><ul><li>📞 +56 61 2 123456</li><li>✉️ info@mirella.cl</li><li>📍 José Menéndez 652, Punta Arenas</li></ul></div>
            <div><h4>Certificaciones</h4><ul><li>ISO 9001 / 14001 / 45001</li><li>Sello Ambiental Región de Magallanes</li></ul></div>
            <div><h4>Síguenos</h4><a href="https://linkedin.com" target="_blank">LinkedIn</a> | <a href="https://x.com" target="_blank">X (Twitter)</a></div>
        </div>
        <div class="footer-bottom">© 2025 Mirella Servicios Marítimos. Todos los derechos reservados.</div>
    </footer>

    <!-- AOS JS -->
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
        AOS.init({ duration: 800, once: true });

        // Mobile menu
        const hamb = document.getElementById('hamburger');
        const menu = document.getElementById('navMenu');
        hamb.addEventListener('click', () => menu.classList.toggle('active'));

        // Smooth scroll
        document.querySelectorAll('a[href^="#"]').forEach(a => {
            a.addEventListener('click', function (e) {
                e.preventDefault();
                const tgt = document.querySelector(this.getAttribute('href'));
                if (tgt) tgt.scrollIntoView({ behavior: 'smooth' });
                menu.classList.remove('active');
            });
        });

        // Form
        document.getElementById('quoteForm').addEventListener('submit', e => {
            e.preventDefault();
            alert('¡Gracias! Nos pondremos en contacto pronto.');
            e.target.reset();
        });
    </script>
</body>
</html>
